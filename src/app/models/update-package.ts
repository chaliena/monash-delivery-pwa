export interface UpdatePackage {
    package_id: string; // Unique identifier for the package
    package_destination: string; // Destination of the package
    // Add any additional properties relevant to your package
  }
  